async def track_order(order_id: str) -> str:
    # Stub — replace with real carrier API call (DHL, FedEx, UPS, etc.)
    return f"Order #{order_id} is currently in transit and expected to arrive within 2–3 business days."
