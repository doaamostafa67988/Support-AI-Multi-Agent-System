# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# LangGraph orchestration
langgraph>=0.2.0
langchain>=0.2.0
langchain-groq>=0.1.6

# Groq SDK (Llama inference)
groq>=0.9.0

# Vector DB & embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.0
httpx>=0.27.0

# NOTE: 'transformers' and 'torch' have been removed.
# Sentiment analysis is now handled by the Groq API directly,
# eliminating ~1 GB of dependencies and 30-60 second cold-start delay.
